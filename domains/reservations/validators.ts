/**
 * @file validators.ts
 * @domain reservations
 * @purpose Zod schemas for handler entry points
 *
 * @ai-notes Used only at handler boundary (handler.ts). Service layer consumes
 *   already-validated input types from types.ts.
 */
export {};
